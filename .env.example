# ==================================================
# Environment Configuration
# ==================================================
# Copy this file to .env.local and fill in the values
# NEVER commit .env.local to version control
# ==================================================

# App Environment
NODE_ENV=development

# ==================================================
# Data Source Configuration
# ==================================================
# Set to "true" to use mock JSON data instead of Supabase
# Default: true in development when SUPABASE_URL is not set
USE_MOCK_DATA=true

# ==================================================
# Supabase Configuration (required for production)
# ==================================================
# Get these from your Supabase project settings

# Public URL (safe to expose to browser)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Anonymous key (safe to expose, used for client-side with RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key (NEVER expose to browser - server-side only)
# This bypasses RLS - use with caution
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ==================================================
# Session Configuration (for production auth)
# ==================================================
# Generate a secure random string for signing JWTs
# Example: openssl rand -base64 32
SESSION_SECRET=your-256-bit-secret-key-here

# Session expiry in seconds (default: 7 days)
SESSION_MAX_AGE=604800

# ==================================================
# Rate Limiting (optional, recommended for production)
# ==================================================
# Using Upstash Redis for rate limiting
# Get these from your Upstash console
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# ==================================================
# Demo Account Credentials
# ==================================================
# Credentials for demo login (not stored in public files)
# Set these in Vercel environment variables for production
DEMO_JUMBO_EMAIL=jumbo@demo.app
DEMO_JUMBO_PASSWORD=change-this-password
DEMO_HITAPES_EMAIL=hitapes@demo.app
DEMO_HITAPES_PASSWORD=change-this-password

# ==================================================
# Feature Flags
# ==================================================
# Enable/disable features for testing
ENABLE_DEBUG_LOGGING=false
